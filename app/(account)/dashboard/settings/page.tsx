import { ReactElement } from "react";

import "./page.css";

export default function SettingsPage(): ReactElement {
    return (
        <main id="settingsPage">
            <h1>Settings</h1>
            
        </main>
    );
}