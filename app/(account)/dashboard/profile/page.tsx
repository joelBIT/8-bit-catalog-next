import { ReactElement } from "react";

import "./page.css";

export default function ProfilePage(): ReactElement {
    return (
        <main id="profilePage">
            <h1>Profile</h1>
        </main>
    );
}