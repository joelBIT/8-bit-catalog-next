import { ReactElement } from "react";

import "./page.css";

export default function AccountProfilePage(): ReactElement {
    return (
        <main id="profilePage">
            <h1>Profile</h1>
        </main>
    );
}