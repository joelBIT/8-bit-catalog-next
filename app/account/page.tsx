import { ReactElement } from "react";

import "./page.css";

export default function AccountPage(): ReactElement {
    return (
        <main id="accountPage">
            <h1>Logged in</h1>
        </main>
    );
}